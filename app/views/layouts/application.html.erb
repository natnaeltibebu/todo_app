<!DOCTYPE html>
<html>
  <head>
    <title>Todo App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', "data-turbo-track": "reload" %>
    <style>
      body { 
        margin: 0;
        font-family: system-ui, -apple-system, sans-serif;
        line-height: 1.5;
      }
      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1rem;
      }
      header {
        background-color: #f3f4f6;
        padding: 1rem 0;
        margin-bottom: 2rem;
      }
      .header-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .stats {
        font-size: 0.875rem;
        color: #4b5563;
      }
      .notice {
        background-color: #d1fae5;
        padding: 1rem;
        margin-bottom: 1rem;
        border-radius: 0.375rem;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin: 1rem 0;
      }
      th, td {
        padding: 0.75rem;
        border: 1px solid #e5e7eb;
      }
      th {
        background-color: #f3f4f6;
      }
      .actions {
        display: flex;
        gap: 0.5rem;
      }
      .button {
        display: inline-block;
        padding: 0.5rem 1rem;
        background-color: #3b82f6;
        color: white;
        text-decoration: none;
        border-radius: 0.375rem;
      }
      .button-red {
        background-color: #ef4444;
      }
    </style>
  </head>
  <body>
    <header>
      <div class="container header-content">
        <div>
          <h1><%= time_based_greeting %></h1>
          <div class="stats">
            <p>Total visits: <%= session[:visit_count] %></p>
            <p>This page visits: <%= session[:page_visits][request.path] %></p>
          </div>
        </div>
        <div class="stats">
          <p>Last visited: <%= time_ago_in_words(session[:last_visited]) %></p>
        </div>
      </div>
    </header>

    <div class="container">
      <% if notice %>
        <div class="notice">
          <%= notice %>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>